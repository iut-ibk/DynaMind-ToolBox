FILE(GLOB PYTHON_CPPS *.cpp)
FILE(GLOB PYTHON_HEADER *.h)

MESSAGE(STATUS SWIG ${CMAKE_BINARY_DIR})

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR} ${CMAKE_BINARY_DIR})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
INCLUDE_DIRECTORIES(../src)

INCLUDE(${SWIG_USE_FILE})

set(CMAKE_SWIG_OUTDIR ${OUTPUT_DIR})

IF(CMAKE_BUILD_TYPE STREQUAL Debug)
        MESSAGE(STATUS "SWIG Warnings enabled")
ELSE()
        MESSAGE(STATUS "SWIG Warnings disabled")
        SET(SWIG_ARGS -w501,473,509,517,401)
ENDIF()

INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR})

SET(CMAKE_SWIG_FLAGS "")
SET_SOURCE_FILES_PROPERTIES(pydmextensions.i PROPERTIES CPLUSPLUS ON)
SET_SOURCE_FILES_PROPERTIES(pydmextensions.i PROPERTIES SWIG_FLAGS "-threads")

SWIG_ADD_MODULE(pydmextensions python pydmextensions.i)
SWIG_LINK_LIBRARIES(pydmextensions dynamind-extensions ${DYNAMIND_CORE_LIBRARY} ${PYTHON_LIBRARIES} ${DYNAMIND_TOOLBOX_LIBRARY} ${DYNAMIND_CORE_LIBRARY} ${QT_LIBRARIES} ${CGAL_LIBRARIES} ${CGAL_3RD_PARTY_LIBRARIES} ${Boost_LIBRARIES})


